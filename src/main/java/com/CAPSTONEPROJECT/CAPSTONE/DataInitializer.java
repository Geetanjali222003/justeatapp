package com.CAPSTONEPROJECT.CAPSTONE;

/**
 * Startup data seeding is intentionally disabled.
 *
 * Authentication must rely only on users explicitly created via /auth/register.
 */
public final class DataInitializer {

    private DataInitializer() {
        // Utility class; no instances.
    }
}
